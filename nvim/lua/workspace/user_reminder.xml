<user-reminder>
  <strict-rule>
    Never chain commands with `&&` or `;` or `|`, or use bash for loops, when each individual command is
    already allowed by `Bash(rg:*)`, `Bash(find:*)` or similar rules. Use native tools or separate
    Bash tool calls instead — parallel if possible.
  </strict-rule>
  <strict-rule>
    Never `cd` into the repo root directory, you are already there.
  </strict-rule>
</user-reminder>

